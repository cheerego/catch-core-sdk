syntax = "proto3";

package hello;

service hello {
    rpc say (pingRequest) returns (pingReply) {
    }
}

message pingRequest {
    string message = 1;
}

message pingReply {
    string message = 1;
}